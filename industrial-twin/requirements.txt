# --- Phase 1: simulate -> model -> scoreboard ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
lightgbm>=4.3
matplotlib>=3.8
joblib>=1.3

# --- Phase 2: explainability + anomaly + rules ---
shap>=0.45

# --- Phase 3: RAG + agents ---
langgraph>=0.1
google-generativeai>=0.7   # Gemini backend (optional; needs GEMINI_API_KEY)
# Ollama is reached over plain HTTP (no client library required).
# Local model used for the offline path: llama3.1:8b
# Optional semantic retrieval:  ollama pull nomic-embed-text
#   then set SENTINEL_EMBED=ollama  (default retrieval is TF-IDF, no download)

# --- Phase 4 (planned): API + serving ---
# fastapi>=0.111
# uvicorn>=0.30
# pydantic>=2.7
