# Secrets — never commit
.env
.env.local
**/.env
!**/.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Node
node_modules/
dist/
.output/
.tanstack/
.wrangler/
.nitro/

# Model weights and downloaded datasets
*.pt
*.pkl
# ...except the trained forecaster: the API is `degraded` without it, so a
# fresh clone needs it to run. See industrial-twin/.gitignore.
!industrial-twin/models/forecaster*.pkl
data/external/

# Scratch
temp.txt
*.log

# Local tooling and editor configuration — not part of the deliverable
.agents/
.claude/
AGENTS.md
.pytest_cache/
